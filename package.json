{
  "name": "specledger",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Powered Industrial Product Intelligence & Catalogue Enrichment Engine",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
